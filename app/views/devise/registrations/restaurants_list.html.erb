<% @restaurants.each do |restaurant|%>
  <%= %>
  	<%= link_to restaurant.name, add_path(restaurant_id: restaurant.id)%><br />	
<% end %>
<%= link_to 'User Sign out', destroy_user_session_path, method: :delete %>
