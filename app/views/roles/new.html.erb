<%= form_for @role  do |role| %>
	<%= role.label :Role %>
	<%= role.select(:user_type, options_for_select([['customer', 'user'], ['Distributor', 'restaurant'], ['Rider', 'delivery_patner']],'user')) %><br />
	<%= role.submit "Add Role" %>
<% end %>