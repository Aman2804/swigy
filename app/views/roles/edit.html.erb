<%= form_for @role do |role| %>
<%= role.select(:user_type, options_for_select([['customer', 'user'], ['Distributor', 'restaurant'], ['Rider', 'delivery patner']],@role.user_type)) %><br />
<%= role.submit 'Save'%>
<% end %>